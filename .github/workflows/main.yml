on:
  push:
    branches:
      - main

jobs:
  main:
    name: main
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true

      - run: bundle install
      - run: bundle exec jekyll build

      - run: bin/rubocop -f github

      # - uses: google-github-actions/auth@v2
      #   with:
      #     credentials_json: ${{ secrets.GCP_SERVICE_ACCOUNT }}

      # - uses: RafikFarhad/push-to-gcr-github-action@v5-rc1
      #   with:
      #     gcloud_service_key: ${{ secrets.GCR_SERVICE_KEY }}
      #     registry: eu.gcr.io
      #     project_id: kollektivavtal
      #     image_name: kollektivavtal
      #     image_tag: latest, ${{ github.sha }}
      #     dockerfile: ./Dockerfile

      # - id: deploy
      #   uses: 'google-github-actions/deploy-cloudrun@v2'
      #   with:
      #     service: 'qubetime'
      #     image: '-docker.pkg.dev/eu.gcr.io/kollektivatal/kollektivavtal:latest'
      #     region: ${{ env.REGION }}

      - uses: peaceiris/actions-gh-pages@v3
        with:
          deploy_key: ${{ secrets.ACTIONS_DEPLOY_KEY }}
          external_repository: kollektivavtal/kollektivavtal.github.io
          force_orphan: true
          publish_branch: main
          publish_dir: ./_site
