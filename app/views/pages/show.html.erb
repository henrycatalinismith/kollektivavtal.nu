<%= render "ui/navbar" %>

<main class="mx-auto flex flex-col items-center">
<div class="flex flex-col gap-10 w-full px-4 lg:px-0 pt-8 max-w-5xl md:flex-row pb-8">
  <div class="prose w-full">
    <%= render_page(@content, @data).html_safe %>
  </div>
</div>
</main>