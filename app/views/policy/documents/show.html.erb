<div class="flex flex-col gap-y-4">

  <div class="flex flex-col gap-y-1">
    <h1 class="font-bold text-4xl m-0">
      <%= policy_revision_title(@revision) %>
    </h1>
  </div>

  <div class="prose w-full">
    <%= render_revision_body(@revision).html_safe %>
  </div>

</div>